# Environment variables for different deployment scenarios

# For SQLite (Development/Small apps)
DATABASE_URL=sqlite+aiosqlite:///./carro.db
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# For PostgreSQL (Production - Railway)
# DATABASE_URL=postgresql+asyncpg://username:password@hostname:port/database

# For PostgreSQL (Production - Heroku style)
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname

# For MySQL (Production)
# DATABASE_URL=mysql+aiomysql://user:pass@host:3306/dbname

# Railway automatically provides DATABASE_URL for PostgreSQL
# Just uncomment the PostgreSQL line above and remove the SQLite line

# Other environment variables
PORT=8000
ENVIRONMENT=production
